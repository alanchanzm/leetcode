import fn from '../src/033';

test(`${fn.name} --- [4,5,6,7,0,1,2],0 --- 4`, () => {
  expect(fn([4, 5, 6, 7, 0, 1, 2], 0)).toBe(4);
});

test(`${fn.name} --- [4,5,6,7,0,1,2],3 --- -1`, () => {
  expect(fn([4, 5, 6, 7, 0, 1, 2], 3)).toBe(-1);
});
